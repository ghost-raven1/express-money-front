import { createSlice } from "@reduxjs/toolkit";
import api from "../api.js";

// TODO: Дописать запросы

const creditScoringSlice = createSlice({
    name: "creditScoring",
    initialState: {},
    reducers: {}
})

export const {} = creditScoringSlice.actions;

export default creditScoringSlice.reducer;