import { createSlice } from "@reduxjs/toolkit";
import api from "../api.js";

// TODO: Дописать запросы

const underwritingSlice = createSlice({
    name: "underwriting",
    initialState: {},
    reducers: {}
})

export const {} = underwritingSlice.actions;

export default underwritingSlice.reducer;