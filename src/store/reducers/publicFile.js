import { createSlice } from "@reduxjs/toolkit";
import api from "../api.js";

// TODO: Дописать запросы

const publicFileSlice = createSlice({
    name: "publicFile",
    initialState: {},
    reducers: {}
})

export const {} = publicFileSlice.actions;

export default publicFileSlice.reducer;